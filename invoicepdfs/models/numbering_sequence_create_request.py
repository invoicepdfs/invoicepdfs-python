# coding: utf-8

"""
    InvoicePDFs

    InvoicePDFs turns a JSON document into a polished PDF — invoices, credit notes, quotes, receipts, proformas, purchase orders and delivery notes. The API validates your data, computes totals correctly (tax, discounts, shipping), and returns a downloadable, professional PDF.  This package is the official client for the InvoicePDFs API, generated from the OpenAPI specification.  Documentation: https://invoicepdfs.com

    The version of the OpenAPI document: 0.2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class NumberingSequenceCreateRequest(BaseModel):
    """
    NumberingSequenceCreateRequest
    """ # noqa: E501
    name: StrictStr
    document_type: Optional[StrictStr] = 'invoice'
    prefix: Optional[StrictStr] = 'INV-'
    date_pattern: Optional[StrictStr] = '{YYYY}-'
    padding: Optional[Annotated[int, Field(le=10, strict=True, ge=1)]] = 5
    next_number: Optional[Annotated[int, Field(strict=True, ge=1)]] = 1
    reset: Optional[StrictStr] = 'yearly'
    __properties: ClassVar[List[str]] = ["name", "document_type", "prefix", "date_pattern", "padding", "next_number", "reset"]

    @field_validator('document_type')
    def document_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['invoice', 'credit_note']):
            raise ValueError("must be one of enum values ('invoice', 'credit_note')")
        return value

    @field_validator('reset')
    def reset_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['never', 'yearly', 'monthly']):
            raise ValueError("must be one of enum values ('never', 'yearly', 'monthly')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NumberingSequenceCreateRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NumberingSequenceCreateRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "document_type": obj.get("document_type") if obj.get("document_type") is not None else 'invoice',
            "prefix": obj.get("prefix") if obj.get("prefix") is not None else 'INV-',
            "date_pattern": obj.get("date_pattern") if obj.get("date_pattern") is not None else '{YYYY}-',
            "padding": obj.get("padding") if obj.get("padding") is not None else 5,
            "next_number": obj.get("next_number") if obj.get("next_number") is not None else 1,
            "reset": obj.get("reset") if obj.get("reset") is not None else 'yearly'
        })
        return _obj


